import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const OrderToBatch = ({navigation}) => {
  return (
    <View>
      <Text>OrderToBatch</Text>
    </View>
  );
};

export default OrderToBatch;

const styles = StyleSheet.create({});
